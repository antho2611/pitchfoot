import SwiftUI

/// Couleurs de marque PitchPro — mêmes valeurs que --pitch / --volt dans src/styles.css (site web).
extension Color {
    static let pitchGreen = Color(red: 0x0d / 255, green: 0x1f / 255, blue: 0x1a / 255)
    static let pitchVolt = Color(red: 0xcc / 255, green: 0xff / 255, blue: 0x00 / 255)
}
